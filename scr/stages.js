class Stages {

    constructor() { // FIXME 
        this.platform = new Platform( 200, 420, 'black' );
        this.key = new Key(50, 320, 'blue');
        this.exit = new Exit( 250, 220, 'green' );
        this.trap = new Trap( 500, 491, 'red' );
    }

    update() { // TODO 

    }

    events() { // TODO 

    }
}